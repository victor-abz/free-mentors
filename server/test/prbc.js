// Export App for App.js
/**
 * mocha lcov reporter
 * install coveralls in the script and when we run the 
 * test it shoul return the ccoverage
 */
/*
import chai from 'chai';
import {expect} from 'chai';
const chaiHttp = require('chai-http');
const app = require('./app');

chai.use(chaiHttp);
const router = () => chai.request(app)

const testPerson = {

}
/**
 * a user cannot sign up twice
 * a user cann;t signup with wrong password
 * a user can't sign up without email */

 /*
chai.use(chaiHttp)

describe('my test suite', () => {
    it('Should return all the users', (done) =>{
        router()
        .get('/')
        .end((error, response) => {
            expect(response).tohave.status(200)
            expect(response.body).to.be.a('object')
            expect(response.body).to.have.property('message')
            expect(response.body.data).to.be.a('array')
        done(error)
        })
    })

    it('Should post a new user', (done) =>{
        router()
        .post('/people')
        .send(testPerson) // testPerson defined above
        .end((error, response) => {
            expect(response).tohave.status(200)
            expect(response.body).to.be.a('object')
            expect(response.body.data).to.be.an('object')
        done(error)
        })
    })
})
*/